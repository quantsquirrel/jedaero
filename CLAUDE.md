# 제대로 (JEDAERO) — 프로젝트 규칙

2026 금융 AI Challenge(금융보안원 주최) 출품작. **제출 마감 2026-09-07 10:00.**
배포 URL이 **2026-09-07 11:00 ~ 09-11 23:59** 열려 있어야 하며, 미접근 시 결격.

전체 명세는 `SPEC.md`. 검증 항목은 `docs/VERIFY.md`. 시드 데이터는 `docs/SEED.md`.
**화면을 만들거나 고치기 전에 `docs/DESIGN-RULES.md`를 읽는다** — 색·크기·부품·오류 문구·반응형 규칙.

---

## 절대 금지 (하나라도 어기면 결격이거나 이용자가 징계받는다)

| # | 금지 | 근거 |
|---|---|---|
| C1 | 카메라 요구 기능 (신분증 촬영, QR, 얼굴인증) | 병사 단말 MDM이 카메라 차단 |
| C2 | GPS / WiFi / 블루투스 API 사용 | 부대관리훈령 [별표1] 30일 휴대폰 사용금지 제재 |
| C3 | 현금성 보상 (포인트 환급, 리퍼럴, 기프티콘) | 동 별표 "온라인 영리행위" 제재. **경험치·배지도 없음** |
| C4 | 소속 부대 정보 수집·저장 (컬럼·입력폼·로그 어디에도) | 군사기밀보호법 시행령 [별표1] 모자이크 위험 |
| C5 | 실제 금융거래·주문·송금·결제 | 자본시장법. 전부 모의 |
| C6 | 실시간 시세 API 호출·체결 알림 | 일별 종가로 충분. 심사 기간 외부 장애 차단 |
| C7 | 연속 미접속을 이탈로 판정 | GOP·훈련으로 수 주 미접속이 정상 |
| C8 | AI가 금융 의사결정을 최종 확정 | 금융분야 AI 가이드라인 ③ 보조수단성 |
| C9 | AI 응답에 생성형 AI 고지 배지 누락 | 금융보안원 AI 보안 안내서 평가기준 1.5 |
| C10 | 투자 추천·종목 리딩·목표가 제시·"최적" 비중 계산 | 유사투자자문 미등록. **"추천" 아니라 "예시"** |
| C11 | 시각(hour) 기준 접속 차단 | 휴가·당직 병사의 접근권. 요일로만 구분 |

---

## 구조상 절대 만들지 말 것

- `holdings` 테이블, `cash_balance` 컬럼 → 보유수량을 저장하지 않는다. 계산한다
- 개별 종목 주문서 UI (수량 입력, 단주 처리, 잔여현금) → 6전선 포인트만
- 비중 조정 슬라이더 → 5%p 단위 `+`/`−` 버튼. 자동 비례 재조정 없음
- 두 곡선 합산 "총 자산" / "총 평가액" 타일 → 곡선은 일시금 하나. 합산 금지
- 백그라운드 크론·스케줄러 → 모든 계산은 요청 시점에
- 인증 라이브러리 (NextAuth 등) → 익명 쿠키 세션만
- "안정형 / 공격형" 같은 투자성향 라벨 → 작전 이름을 쓴다
- 가계부·퀘스트·XP 테이블과 라우트 → 되살리지 말 것

## 반드시 지킬 것

- `allocations` 에 `UNIQUE(user_id, week_of)` → 주 1회·수정 불가를 DB가 강제
- 요일 판정은 **서버에서.** 클라이언트 시계를 신뢰하지 않는다
- LLM 호출은 서버 라우트/서버 액션에서만. 클라이언트 번들에 키 노출 금지
- 금액은 **원 단위 정수.** 부동소수 금지
- 에러 페이지에 스택트레이스 노출 금지
- 색은 토큰으로만 쓴다. `zinc-*`·`amber-*` 같은 리터럴 클래스 금지 (`docs/DESIGN-RULES.md` §1)
- 손가락으로 누르는 것은 44px 이상. 버튼 `h-11`, 입력창 `h-12`, 스테퍼 `size-10` (동 §3)

---

## 작업 방식

- 요구사항을 충족하는 **가장 단순한 구현**을 고른다. 투기적 추상화·설정·간접층을 만들지 않는다
- **end-to-end로 동작하는 최소 버전에서 시작해 층으로 올린다.** 동작하는 제품을 미완성 복잡도와 바꾸지 않는다
- `main`에 직접 커밋하지 않는다. 작업 브랜치를 쓴다
- `.env`, `credentials.*` 커밋 금지
- 명세가 모순되거나 불명확하면 **추측해서 메우지 말고 물어본다**
- 테스트·검증 결과를 보고할 때는 **실행한 명령과 그 원문 출력을 함께** 남긴다

## 스택 (고정)

```
Next.js 15 App Router + TypeScript + Tailwind + shadcn/ui
PostgreSQL (Neon) + Drizzle ORM
Vercel 배포
LLM: Anthropic Claude API 또는 OpenAI — 서버에서만 호출
시세: 더미 시드 데이터 (외부 API 런타임 의존 없음)
```
