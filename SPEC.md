# 제대로 (JEDAERO) — 구현 명세

> 2026 금융 AI Challenge (금융보안원 주최) 출품작
> **제출 마감 2026-09-07 10:00 / 배포 URL은 09-07 11:00 ~ 09-11 23:59 접근 가능해야 함. 미접근 시 결격.**
>
> 하드 제약은 `CLAUDE.md`. 검증 항목은 `docs/VERIFY.md`. 시드 데이터는 `docs/SEED.md`.
> 설계 근거는 `docs/DESIGN-DECISIONS.md`. 가계부·퀘스트는 **제외·제거 완료** — 이 문서에 되살리지 말 것.
> 세션·기록 범위(저장 / 요청 시 계산 / 저장하지 않음)는 §1-1 · §2-1.

---

## 0. 한 줄 정의

> **전역할 때 받게 될 목돈을, 복무 중에 미리 굴려보는 모의투자 훈련 웹서비스.**
> 시드는 전원 **2,000만원**. 실제 만기 수령액은 사람마다 달라서 화면에 쓰지 않는다.

병사는 평일 18:00~21:00, 휴일 08:30~21:00에만 휴대전화를 쓸 수 있고, 국내 정규장(09:00~15:30)과 1분도 겹치지 않는다. 장중에 시세를 볼 수 없으니 패닉 매매가 구조적으로 불가능하다.

**군대는 이미 최고의 장기투자 훈련 환경이며, 아무도 이것을 훈련으로 설계하지 않았을 뿐이다.**

핵심 문법 하나가 전 기능을 관통한다.

> **결정은 미리 하고, 중간에 바꾸지 않는다.**
> 편성은 주말에 정하고 평일에는 못 바꾼다.

---

## 1. 기술 스택

```
Framework   Next.js 15 (App Router) + TypeScript
UI          Tailwind CSS + shadcn/ui
DB          PostgreSQL (Neon 무료 티어) + Drizzle ORM
Auth        익명 쿠키 세션 (인증 라이브러리 없음)
LLM         Anthropic Claude API 또는 OpenAI. 서버 라우트/서버 액션에서만 호출
배포        Vercel
시세        더미 시드 데이터. 런타임 외부 API 의존 없음
```

### 1-1. 익명 세션

로그인이 없는 것과 데이터베이스가 없는 것은 다르다. **DB는 필수다. 로그인만 두지 않는다.**

- 온보딩 또는 `/demo`가 `users` 행을 만들고, 브라우저에 `user_id` 쿠키를 붙인다
- 쿠키: httpOnly, secure(프로덕션), sameSite=lax, maxAge 180일. 구현은 `lib/session.ts`
- 이메일·비밀번호·NextAuth 없음. 회원가입·로그인 화면 없음. 랜딩에서 `/demo` 또는 `/onboarding`
- 쿠키를 지우거나 기기를 바꾸면 그 `users.id`의 편성 이력에 이어서 들어오지 못한다. 계정 복구는 출품 범위 밖
- `/admin`만 환경변수 `ADMIN_PASSWORD` 단순 비교
- `DATABASE_URL`(Neon)이 없으면 세션·편성·지수가 전부 동작하지 않는다. 로컬 `.env`가 비어 있으면 `/demo`가 죽는 것이 정상이다

**로그인을 뺀 이유**는 MVP 생략이 아니다. 병사 단말에서 가입 마찰을 없애고, 실명·연락처·부대 정보를 받지 않기 위한 제품 결정이다 (C4). 신원은 계정이 아니라 **편성 이력**이다. 근거는 `docs/DESIGN-DECISIONS.md` §15.

### 1-2. 백그라운드 작업을 쓰지 않는 이유

Vercel 크론이 가능하지만 쓰지 않는다. 주간 시즌 리셋, 곡선, 제대로 지수 집계를 전부 **요청 시점 계산**으로 둔다.

① 심사 기간에 크론이 실패해도 서비스가 멀쩡해야 한다 ② 계산량이 사소하다(사용자당 주 1행 × 18주)

---

## 2. 데이터 모델

> **부대 관련 컬럼이 하나도 없다는 점이 이 스키마의 핵심이다. 절대 추가하지 말 것.**
> 확정본은 `db/schema.ts`. **테이블 11개.** 가계부·퀘스트 테이블을 되살리지 말 것.

```sql
users (
  id                uuid PK,
  nickname          text NOT NULL,        -- 실명 입력 금지 안내
  rank              text NOT NULL,        -- PRIVATE|PFC|CORPORAL|SERGEANT
  branch            text NOT NULL,        -- ARMY|NAVY|AIRFORCE|MARINE
  enlisted_at       date NOT NULL,
  discharge_at      date NOT NULL,
  home_distance     text NOT NULL,        -- NEAR|MID|FAR|ISLAND (주소 원문 저장 금지)
  analytics_opt_in  boolean NOT NULL DEFAULT false,   -- AI-7
  is_demo           boolean NOT NULL DEFAULT false,   -- /demo가 만든 체험 계정만 true
  created_at        timestamptz NOT NULL
)

-- ★ 사용자가 조작하는 유일한 대상: 주차별 목표 비중 이력
allocations (
  id              uuid PK,
  user_id         uuid FK -> users.id,
  week_of         text NOT NULL,          -- YYYY-WW
  weights         jsonb NOT NULL,         -- 전선별 % 정수. 5의 배수, 합계 100 이하(나머지=예비대)
  details         jsonb,                  -- 하위 테마(주식 2전선만). NULL이면 대표지수 추종
  template_id     text,                   -- 예시 작전 id
  decided_at      timestamptz NOT NULL,
  effective_from  date NOT NULL,          -- 체결 기준일 = 다음 거래일
  UNIQUE(user_id, week_of)
)

-- 평일 명령하달 초안. 체결이 아님. 안 적은 주가 정상.
drafts (
  id          uuid PK,
  user_id     uuid FK -> users.id,
  week_of     text NOT NULL,
  weights     jsonb NOT NULL,
  note        text,
  updated_at  timestamptz NOT NULL,
  UNIQUE(user_id, week_of)
)

groups (
  id uuid PK, name text NOT NULL,          -- AI-5 필터 통과분만
  invite_code text UNIQUE NOT NULL,
  owner_id uuid, member_limit integer NOT NULL DEFAULT 30
)
group_members (id uuid PK, group_id uuid, user_id uuid)

-- 제대로 지수. 요청 시점 lazy upsert. 크론 없음
weekly_scores (
  id uuid PK, user_id uuid, week_of text,
  grown real,   -- 위험을 이긴 성과 (40)
  spread real,  -- 분산의 힘 (30)
  held real,    -- 판단을 지킨 힘 (30)
  total real    -- 100점
)

tickers (ticker text PK, name text, theme text, kind text)  -- kind: INDEX|THEME
prices  (ticker text, trade_date date, close integer, PRIMARY KEY(ticker, trade_date))
holidays (holiday_date date PK, name text)
settings (key text PK, value text)   -- settings('ai_enabled', 'true'|'false')
ai_calls (
  id uuid PK, user_id uuid,
  kind text NOT NULL,           -- AI-3|AI-4|AI-5|AI-7|AI-8
  blocked boolean NOT NULL DEFAULT false,
  created_at timestamptz NOT NULL
)
```

> **없는 것**: `holdings`, `cash_balance`. 보유수량은 (비중 이력 × 가격)으로 요청 시점에 계산한다. 현금은 **예비대**(미배치 포인트)다. `quests`·`quest_progress`·`budget_*`·`expenses`·`exemption_claims` 없음.

### 2-1. 무엇을 기록하는가

기획서·기능 명세서에 옮길 때 이 표를 기준으로 한다. 「사용자 정보 / 수익률 / 판단」을 한 덩어리로 저장하지 않는다.

| 종류 | 처리 | 위치 |
|---|---|---|
| 별명·계급·군종·입대일·전역예정일·거리 구간 | **저장** | `users`. 실명·주소·부대 원문 없음 (C4) |
| 판단 — 주말 편성 확정 | **저장** | `allocations` 주 1행. `UNIQUE(user_id, week_of)`가 수정 불가·주 1회를 DB에서 강제 |
| 판단 — 평일 명령하달 초안 | **저장** | `drafts`. 체결이 아닌 메모. 안 적은 주가 정상 |
| 그룹 소속 | **저장** | `groups` / `group_members`. 초대코드. 수익 금액은 이 화면에 없음 |
| 성향 분석 동의 | **저장** | `users.analytics_opt_in`. 기본 false |
| 시세 | **저장** (시드) | `tickers` / `prices`. 더미 일별 종가. 런타임 시세 API 없음 (C6) |
| 제대로 지수 주간 점수 | **요청 때 계산 후 lazy upsert** | `weekly_scores`. 크론 없음 |
| 수익률·평가액·보유수량 | **저장하지 않음. 요청 때 계산** | 비중 이력 × 종가 (`lib/portfolio/engine.ts`) |
| 한 줄 회고 (AI-3) 본문 | **저장하지 않음** | 사실 1~2문장 + 질문만 돌려주고 버린다 |
| AI 호출 여부 | **저장** (운영) | `ai_calls`. 분당/일 한도·킬스위치 로그. 사용자의 판단 기록이 아님 |

온보딩에서 「이 배분을 고른 이유 한 줄」은 선택 입력이며 스키마에 컬럼이 없어 저장하지 않는다.

---

## 3. 핵심 로직

### 3-1. 시드와 계급

모의 시드 `SEED_AMOUNT = 20_000_000`. 전원 동일.

계급(`Rank`)은 온보딩에서 받고, 제대로 지수 「같은 계급」 탭에 쓴다. **월 봉급 숫자는 화면에 띄우지 않는다.** 가계부를 뺀 뒤 용도가 없다. `SALARY_2026`·`TRANSPORT_CAP` 상수는 코드에 남아 있을 수 있으나 제품 흐름에 쓰지 않는다.

### 3-2. 가계부 — 제외

봉투 예산·지출 분류·면제 청구·「매달 모았다면」 적립 곡선은 **제외했다** (`DESIGN-DECISIONS.md` §9). 라우트 `/expenses`, `/budget` 없음.

### 3-3. 퀘스트·XP — 제거

접속 빈도를 늘리는 게임화라 제거했다 (`DESIGN-DECISIONS.md` §7). 「아무것도 하지 않는 것도 유효」는 미조정 시 행이 생기지 않는 구조가 잇는다.

### 3-4. 요일 기반 기능 구분 ★

판정은 **서버에서.** 클라이언트 시계를 신뢰하지 않는다. 시각(hour)으로 접속을 차단하지 않는다 (C11). 일요일 21:00 KST는 편성 마감이지 접속 차단이 아니다.

| 기능 | 평일 | 주말·공휴일 |
|---|---|---|
| 접속·학습·한 줄 회고 | ✅ | ✅ |
| 전선 등락 (시황) | ✅ 홈·학습. 내 손익 가중 없음 | ✅ 내 비중 가중 + AI 브리핑 |
| 누적 수익률 | ✅ 홈·포트폴리오 | ✅ |
| 이번 주 변동 | ❌ | ✅ 홈·포트폴리오·학습 |
| **편성 조정** | **잠김** | ✅ 주 1회, 일 21:00 마감 |
| **명령하달 (초안)** | ✅ 선택. 체결 아님 | 읽기 전용 — 편성기에서 나란히 |
| 제대로 지수·성향 분석 | 잠김 (출구: 학습·그룹) | ✅ 홈·지수 |
| 그룹 | ✅ | ✅ |

> 평일에도 누적(느린 숫자)은 본다. 이번 주가 얼마나 흔들렸는지는 주말에 본다 (`DESIGN-DECISIONS.md` §5 지표 교체).
> 조정 잠금과 지표 교체는 별개다. 편성은 주말에만 바꾼다.

데모 세션은 화면 최상단 토글로 요일을 바꾼다. 토글에 **주말에서 무엇이 열리는지**를 적는다. 심사 기간 5일이 전부 평일이라 토글은 결격 방지 장치다.

### 3-5. 편성 규칙

| 논점 | 규칙 |
|---|---|
| 빈도 | **주 1회.** `UNIQUE(user_id, week_of)`가 DB에서 강제 |
| 마감 | **일요일 21:00 KST** |
| 확정 후 수정 | **불가** |
| 미조정 시 | **기존 편성 유지.** 행이 없는 주 = 유지 |
| 체결 | **다음 거래일 종가** |
| 조작 | 포인트 20개, 1포인트 = 5%p. 슬라이더 금지. `+`/`−`만 |
| 재조정 | **자동 비례 재조정 없음.** 한 전선을 올려도 다른 전선은 그대로. 예비대가 줄어든다 |
| 예비대 | 미배치분. 화면에 항상 표시 |

UI에 마감 카운트다운을 띄우되 **"조정하지 않으면 기존 편성이 그대로 유지됩니다"를 같은 화면·같은 크기로** 함께 표시한다.

결정(주말)과 체결(다음 거래일 종가) 사이의 시차는 버그가 아니라 **예약주문의 본질**이다.

### 3-5-A. 명령하달 — 평일 초안 (선택)

평일에 6전선 목표 비중 초안과 근거 한 줄을 남길 수 있다. **체결되지 않는다.** `drafts` 행이며 `allocations`를 건드리지 않아 주 1회 규율은 그대로다.

- **안 적은 주가 정상이다.** 빈 상태를 꾸짖는 배지·카운터·「미작성」 표기를 만들지 않는다 (C7)
- 평일 동안 수정·삭제 가능. 그 주 편성을 확정하면 읽기 전용
- 주말 편성기에 초안과 지금 값을 나란히 보여주고 **「초안 불러오기」는 한 번**. **자동 적용 금지** — 불러오는 것 자체가 명령이 되면 안 된다
- 확정 후 초안과 다르면 **규칙 기반 사실 한 문장**. 조언·평가를 붙이지 않는다 (C8, C10)
- 근거 한 줄은 **DB에 저장되므로** 인젝션·개인정보·부대정보 필터를 반드시 통과시킨다 (C4)
- UI에 「예약 주문」이라고 쓰지 않는다. **「아직 실행되지 않은 메모」**

### 3-6. 포트폴리오 ★

사용자가 조작하는 대상은 **6전선 포인트** 하나뿐이다.

| 코드 | 이름 | 하위 테마 |
|---|---|---|
| `KR_STOCK` | 국내 주식 | ✅ 2포인트 이상일 때 |
| `US_STOCK` | 미국 주식 | ✅ 2포인트 이상일 때 |
| `INTL_STOCK` | 기타 해외 | ❌ 대표지수 |
| `BOND` | 채권 | ❌ |
| `GOLD_COMM` | 금·원자재 | ❌ |
| `REIT_INFRA` | 리츠·인프라 | ❌ |

하위 미지정 = 그 전선의 **대표지수 추종**. 동일가중이 아니다. 상위 포인트를 줄이면 하위 배치는 버리고 지수 추종으로 돌아간다. 비례 재조정 없음.

온보딩 예시 작전 5종: 연합작전 · 돌파 · 종심 방어 · 거점 확보 · 정찰 침투. **"추천"이 아니라 "예시"** (C10). 각 카드에 누적·MDD·장점 2·단점 2를 같은 무게로. 배분표는 `docs/SEED.md`.

곡선은 **전역(일시금) 하나**. 「매달 모았다면」은 가계부와 함께 폐지. 두 곡선을 합산한 "총 자산"/"총 평가액"을 만들지 말 것.

포트폴리오에 **목표 vs 현재 비중** 갭을 항상 표시하고, 주말에 **「목표대로 되돌리기」**를 둔다. 갭은 소수 첫째자리 %p.

### 3-7. 제대로 지수

등수 숫자를 만들지 않는다. 목록은 **가입순**. 수익 금액은 표시하지 않는다.

| 배점 | 이름 | 요지 |
|---|---|---|
| 40 | 위험을 이긴 성과 | 위험 대비 수익 (샤프, rf=0 — 예비대가 무위험) |
| 30 | 분산의 힘 | 유효 전선 수 = 1/Σ(비중²). 예비대도 한 몫 |
| 30 | 판단을 지킨 힘 | 목표 비중 변화량의 역수. 리밸런싱(같은 목표 재확정)은 Δ=0 |

비교 집단: **우리 그룹 / 같은 군종 / 같은 계급**. 전역월 코호트 없음. 주간 시즌, 누적 순위 없음 (C7).

### 3-8. 학습 카드 — 기능과 1:1 페어링

| 단계 | 기능 | 제목 |
|---|---|---|
| 1 | 모의 시드 | 적금 만기액은 어떻게 나오나 |
| 2 | 예시 선택 / 전선 편성 | 분산이 줄이는 것과 줄이지 못하는 것 |
| 3 | 평일 누적 / 주말 주간 변동 | 매일 보는 숫자와 주 단위로 보는 숫자는 다르다 |
| 4 | 주말 편성 조정 | 리밸런싱의 정의 |
| 5 | 한 줄 회고 + 성향 분석 | 수익률에는 두 종류가 있다 |

각 카드는 대응 기능 화면에서 진입 가능해야 한다 (`#card-seed` 등).

---

## 4. AI 적용 지점

모든 LLM 호출은 서버에서만. 클라이언트에서 직접 부르면 안 된다.

| # | 상태 | 기능 |
|---|---|---|
| AI-1 | **폐지** | 지출 분류 (가계부 제외) |
| AI-2 | **폐지** | 봉투 제안 (가계부 제외) |
| **AI-3** | 유지 | 한 줄 회고 → 사실 1~2문장 + 열린 질문 1개. 저장하지 않음 |
| **AI-4** | 유지 | 규칙으로 등락을 계산한 뒤 LLM이 요약 + 질문 3개. 전망·추천 폐기. 내 비중 가중·브리핑 버튼은 **주말** |
| **AI-5** | 유지 | 그룹명 부대정보 차단. 정규식 1차 + LLM 가드 2차 |
| AI-6 | **폐지** | 면제 패턴 상담 연결 (가계부 제외) |
| **AI-7** | 유지 | 집단 성향 분석. 옵트인 + 상호주의 + k-익명성(n<20) |
| **AI-8** | 유지 | 나의 투자 원칙 — 내 배분과 공표 기관 배분의 «차이의 이유»를 목적·기간으로 서술. 어느 배분이 낫다고 말하지 않는다 |

### AI-4 — 뉴스가 아니라 가격에서 출발한다

숫자는 `lib/market-week.ts`가 먼저 계산한다. LLM은 그 숫자를 받아 문장만 만든다.

- 기사 본문을 넣지 않는다. 시드는 합성 데이터라 실제 기사와 맞물리면 모순이 된다.
- 평일 홈·학습에는 **시황 등락표**만 연다. 내 손익 가중은 주말.

### AI-5 — 그룹명 필터

개별 병사 1인의 소속은 군사기밀이 아니지만, 다수로부터 수집해 부대별 통계를 산출하면 편제 현황에 근접한다.

### AI-3 — 1턴으로 끝낸다

답을 적어 보내는 칸을 두지 않는다. 답은 다음 주 결정으로 한다.

### AI-7 — 옵트인 + 상호주의

동의해야 집계에 포함. 거부하면 비교 화면도 안 보인다. 표시: 전선 편성 / 집중도 / 회전율 / 예비대 / 변동성.

**출력은 사실 서술 + 질문으로 끝난다 (C8, C10).**

`lib/ai/output-guard.ts`를 AI-3·AI-4·AI-7·AI-8이 공유한다. 조언 어미, 성향 라벨, 전망·매매 방향이 감지되면 출력을 폐기하고 규칙 기반 템플릿으로 폴백한다. **프롬프트로만 막지 않는다.**

---

## 5. 보안 구현

| 항목 | 구현 |
|---|---|
| 입력 필터링 | LLM 호출 전 인젝션·탈옥 패턴 차단 |
| 개인정보·부대정보 | 주민등록번호·계좌·부대 패턴을 같은 파이프라인 |
| 출력 검증 | 시스템 프롬프트 미노출. 스택트레이스 금지 |
| 호출 횟수 제한 | 분당 5회, 일 50회. 초과 시 429 |
| 킬스위치 | `/admin`에서 `ai_enabled=false` → 룰 기반 폴백 |
| 생성형 AI 고지 | AI 응답 상단 배지. 규칙 기반(등락 계산·요일·지수 산식)은 「AI 아님」 구분 |

---

## 6. 화면

| # | 라우트 | 화면 | 핵심 요소 |
|---|---|---|---|
| S1 | `/` | 랜딩 | 히어로 + 스크롤 설명. **[3분 심사 데모 시작]** / **[시작하기]** |
| S2 | `/onboarding` | 온보딩 | 별명·계급·군종·날짜·거리 구간 → 시드 → 예시 작전 5종 |
| S3 | `/home` | 홈 | 다음 편성까지 D-n. 오늘의 지형(전선 등락). **평일=누적 수익률 / 주말=평가액+이번 주 변동+제대로 지수** (지표 교체) |
| S4 | `/portfolio` | 포트폴리오 | 포인트 20 + 예비대 + 하위 테마. 갭. 평일=누적, 주말=평가액+이번 주 |
| S7 | `/league` | 제대로 지수 | 3축 막대. 3탭. 등수 없음. 하단 네비 라벨 「지수」 |
| S8 | `/learn` | 학습 | 5카드 + 등락표 + 한 줄 회고 |
| S9 | `/insights` | 성향 분석 | AI-7. 옵트인. 주말에만 비교 |
| S10 | `/groups` | 그룹 | 초대코드, 정원 30. 하단 네비 없음(홈·지수에서 진입) |
| S11 | `/admin` | 관리자 | 킬스위치, 호출 로그 |
| S12 | `/principles` | 나의 투자 원칙 | 측정된 사실 문장 + 공표 기관 배분 비교 + AI-8. 이미지 저장. 하단 네비 없음(홈·학습에서 진입) |
| — | `/demo` | 데모 진입 | §7 |

`/expenses`, `/budget` 없음.

하단 네비: 홈 · 포트폴리오 · 지수 · 학습.

### UI 원칙

- 앱 영역은 모바일 폭(`max-w-md`). 랜딩은 전폭
- 잠금 상태를 숨기지 말고 이유를 설명한다
- 모든 생성형 AI 응답 상단에 고지 배지
- 다크모드 기본
- 한글, 금액은 원 단위 정수·천 단위 콤마

---

## 7. 데모 모드 ★

심사는 **2026-09-07 11:00 ~ 09-11 23:59**. 5일 전부 평일. 토글 없이 주말 기능을 못 보면 결격에 준한다.

```
/demo 진입 시:
  - 익명 세션 + 데모 시드 (12주 편성 이력, 조정 안 한 주 포함)
  - 최상단 요일 토글. 주말에서 열리는 것을 문구로 명시
  - 리그·성향용 더미 사용자 200명
```

심사자 시나리오는 `docs/VERIFY.md` §B.

---

## 8. 구현 상태 (2026-09-03)

동작 중: 랜딩·데모 토글·온보딩·포인트 편성·예비대·하위 테마·지표 교체·오늘의 지형·제대로 지수(홈·지수)·학습 카드·**도상훈련 3장**·**명령하달 초안**·AI-3·AI-4(평일/주말 분리)·AI-5·AI-7·**나의 투자 원칙(AI-8)**·출력 가드·**숫자 가드**·킬스위치.

미착수(배포 이후): 브리핑룸 본편(정찰 일지), 6전선 종가 «적재»(맵·검증은 완료, `db/seed/krx-map.ts`·P1-20), 초안↔확정 차이의 AI 문장(현재는 규칙 기반).

하지 않음: 공익 군종(타겟 아님 — 장중 폰 사용 가능). 설정 탭은 온보딩 필드로 충분. 커스텀 구간 도상훈련은 상용화 확장(§10).

P0 14 / P1 21 자동 검증은 `docs/VERIFY.md`. `DATABASE_URL` 없으면 DB 항목은 FAIL이 정상이다.

---

## 9. 일정

| 날짜 | 목표 |
|---|---|
| 09-04 | Neon·푸시·Vercel. `/demo` 눈으로 확인. 보안 PR 머지 |
| 09-06 | 기능 동결. 문서 = 제품 |
| 09-07 | **10:00 이전 제출.** 배포 URL 09-07 11:00 부터 열려 있어야 함 |

**마감 당일 배포하지 않는다.** 09-07 ~ 09-11 매일 URL 접속 확인.

---

## 10. 확장 (출품 범위 밖)

- **커스텀 도상훈련** — 사용자가 시작·종료일을 고른다. 전선 단위만. 개별 종목 검색 없음. 최소 60영업일. 1차 UI 명칭은 여전히 「도상훈련」.
- 시세 지형 일러스트(전선당 추상 이미지)는 맵·캐시 다음 시각 작업으로 둔다. 상품 로고·캔들 금지.
