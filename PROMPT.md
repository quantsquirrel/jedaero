# Claude Code 프롬프트

아래 `---` 아래 전체를 복사해서 Claude Code에 붙여넣으세요.

---

이 폴더에서 "제대로(JEDAERO)" 웹서비스를 만들어줘.

## 먼저 읽을 것

- `CLAUDE.md` — 절대 금지 사항 11개(C1~C11)와 구조 규칙. 협상 불가야.
- `SPEC.md` — 전체 구현 명세.
- `docs/SEED.md` — 예시 포트폴리오, 종목, 가격 시드 요구사항, 퀘스트, 상수.
- `docs/VERIFY.md` — 검증 항목 P0 14개 / P1 13개, 심사자 시나리오 18개.

## 배경

2026 금융 AI Challenge(금융보안원 주최) 출품작이야. 제출 마감은 9월 7일 10시고,
배포 URL이 9월 7일 11시부터 9월 11일 23시 59분까지 열려 있어야 해. 안 열리면 결격이야.
심사자가 평일 낮에 접속해도 전 기능을 볼 수 있어야 해서 `/demo` 요일 토글이 필수야.

## 진행 방식

한 번에 전부 만들지 말고 아래 3단계로 나눠서, **각 단계 끝에서 멈추고 나에게 보고해줘.**

### 1단계 — 골격

Next.js 15 + TS + Tailwind + shadcn + Drizzle + Neon 연결, 랜딩 한 장, Vercel 배포.
`SPEC.md` 2절 스키마 15개 테이블을 Drizzle로 옮기고 마이그레이션 적용.
`docs/SEED.md` 3절 요구사항대로 가격 시드 생성 스크립트 작성 및 실행.
`scripts/verify.sh` 를 `docs/VERIFY.md` 형식대로 작성 (이 단계에선 FAIL이 있어도 됨).

**멈추고 보고**: `npm run build` 출력, 테이블 목록 쿼리 결과, 배포 URL과 curl 상태코드,
`bash scripts/verify.sh` 출력 전문.

### 2단계 — P0

`SPEC.md` 8절의 P0 여섯 항목. P1/P2는 손대지 마.

**멈추고 보고**: `bash scripts/verify.sh` 출력 전문. P0 14개가 전부 PASS여야 해.
FAIL이 남으면 항목 번호와 이유를 알려줘.

### 3단계 — P1

`SPEC.md` 8절의 P1 아홉 항목. P2는 손대지 마.

**멈추고 보고**: `bash scripts/verify.sh` 출력 전문. P0 회귀가 없어야 해.

## 특히 틀리기 쉬운 것 5개

명세에 다 있지만 반대로 구현하기 쉬운 것들이라 여기 다시 적을게.

1. **`holdings` 테이블을 만들지 마.** 보유 수량을 저장하지 않고
   (비중 이력 × 일별 종가 × 현금흐름)으로 요청 시점에 계산해.
   개별 종목 주문서 UI(수량 입력, 단주, 잔여현금)도 만들지 마.
   사용자가 조작하는 건 테마 6축 비중 하나뿐이야. 5%p 단위 +/- 버튼으로.

2. **시각(hour)으로 접속을 차단하지 마.** 요일만 봐.
   평일에도 로그인·지출 기록·학습은 가능해야 하고, 평일에 잠기는 건
   수익률 표시와 비중 조정 두 가지뿐이야. 휴가·당직 병사가 있어서 그래.

3. **「매달 모았다면」은 두 번째 포트폴리오가 아니야.**
   같은 비중 이력과 같은 가격에 현금흐름 배열만 바꿔 넣어서 곡선을 하나 더 그리는 거야.
   DB에 저장하지 말고 요청 시점에 계산해.
   그리고 **두 곡선을 합산해서 "총 자산"으로 표시하면 안 돼.** 같은 돈을 두 번 세는 거야.

4. **예시 포트폴리오를 "추천"이라고 부르지 마.** "예시"야.
   "안정형/공격형" 같은 투자성향 라벨도 쓰지 마. 우리는 유사투자자문 미등록이라
   추천·목표가·최적 비중 계산을 하면 안 돼. AI는 사실 서술과 질문까지만 해.

5. **가격 시드 중간에 -15% 이상 하락 구간을 반드시 넣어.**
   상승만 하면 3번의 두 곡선이 거의 겹쳐서 교육 효과가 0이 돼.

## 지금 할 일

코드를 쓰기 전에 먼저 이것부터 해줘:

1. 문서 4개를 읽고 1단계의 파일 구조와 라우트 목록을 제안해줘.
2. `scripts/verify.sh` 의 P0 14개 항목을 각각 어떤 명령으로 검사할지 한 줄씩 적어줘.
3. 명세에서 모순되거나 불명확한 부분이 있으면 목록으로 알려줘. **추측해서 메우지 마.**
4. Neon·Vercel·LLM API 키 중 내가 직접 해야 하는 게 있으면 먼저 알려줘.

승인하면 1단계를 시작할게.
