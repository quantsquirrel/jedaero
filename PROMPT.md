# Claude Code 프롬프트

이 레포는 **이미 구현된 출품작**이다. 처음부터 만들지 마라.
새 세션은 `docs/HANDOFF.md`를 먼저 읽고, 하드 제약은 `CLAUDE.md`다.

아래는 과거 부트스트랩용이다. 스키마 15개·퀘스트·가계부·2,020만은 **구버전**이다. 따르지 마라.

---

이 폴더의 "제대로(JEDAERO)"를 **이어서** 작업해줘.

## 먼저 읽을 것

- `CLAUDE.md` — 절대 금지 C1~C11. 협상 불가.
- `docs/HANDOFF.md` — 지금 어디고 다음에 뭘 하나.
- `SPEC.md` — 현재 구현 명세 (가계부·퀘스트 없음. 시드 2,000만원. 테이블 10개).
- `docs/DESIGN-DECISIONS.md` — 왜 그렇게 만들었나.
- `docs/SEED.md` — 예시 작전·지수 시드·가격 조건.
- `docs/VERIFY.md` — P0 14 / P1 (폐지 2개 포함) / 심사자 시나리오.

## 배경

2026 금융 AI Challenge(금융보안원 주최). 제출 마감 9월 7일 10시.
배포 URL이 9월 7일 11시 ~ 9월 11일 23:59에 열려 있어야 한다. 안 열리면 결격.
심사 5일이 전부 평일이라 `/demo` 요일 토글이 결격 방지 장치다.

## 특히 틀리기 쉬운 것

1. **`holdings` / `cash_balance` 만들지 마.** 보유수량은 계산한다. 주문서 UI 금지. 조작은 6전선 포인트, 5%p `+`/`−`. 자동 비례 재조정 없음. 예비대를 항상 보여라.
2. **시각으로 접속 차단하지 마.** 요일만. 평일에도 학습·시황·누적 수익률은 된다. 잠기는 것은 편성 조정과 이번 주 변동·지수 비교다.
3. **「매달 모았다면」·가계부·퀘스트를 되살리지 마.** 곡선은 일시금 하나. "총 자산" 합산 금지.
4. **예시를 "추천"이라고 부르지 마.** 작전명만. AI는 사실 서술과 질문. `output-guard`가 조언·라벨·전망을 폐기한다.
5. **테이블은 10개다.** `quests`·예산·지출 테이블을 만들지 마.

## 배포

에이전트는 Vercel 대시보드를 대신할 수 없다. Neon `DATABASE_URL`이 없으면 빌드·`/demo`가 실패한다. 로컬 TCP Postgres는 `neon-http`에 안 붙는다.
