// 화면당 «하나»의 들린 표면 띠.
// 「밝기가 곧 상태다」 — 이 화면의 작업 대상이 무엇인지를 색조나 테두리가 아니라
// 표면 한 단(bg-card → bg-secondary)으로 말한다. 한 화면에 두 번 쓰지 않는다.
// ★ 신호색(--primary)은 여기 쓰지 않는다. 이것은 «위계»이지 «행동»이 아니다.
export const LEAD_BAND = 'bg-secondary ring-foreground/15';
