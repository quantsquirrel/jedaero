# 제대로 (JEDAERO) — 구현 명세

> 2026 금융 AI Challenge (금융보안원 주최) 출품작
> **제출 마감 2026-09-07 10:00 / 배포 URL은 09-07 11:00 ~ 09-11 23:59 접근 가능해야 함. 미접근 시 결격.**
>
> 하드 제약은 `CLAUDE.md`. 검증 항목은 `docs/VERIFY.md`. 시드 데이터는 `docs/SEED.md`.

---

## 0. 한 줄 정의

> **전역할 때 실제로 받게 될 2,020만원을, 복무 중에 미리 굴려보는 모의투자 훈련 웹서비스.**

병사는 평일 18:00~21:00, 휴일 08:30~21:00에만 휴대전화를 쓸 수 있고, 국내 정규장(09:00~15:30)과 1분도 겹치지 않는다. 장중에 시세를 볼 수 없으니 패닉 매매가 구조적으로 불가능하다.

**군대는 이미 최고의 장기투자 훈련 환경이며, 아무도 이것을 훈련으로 설계하지 않았을 뿐이다.**

핵심 문법 하나가 전 기능을 관통한다.

> **결정은 미리 하고, 중간에 바꾸지 않는다.**
> 포트폴리오는 주말에 정하고 주중에 못 바꾼다. 예산 봉투는 월초에 정하고 그 달에 못 바꾼다.

---

## 1. 기술 스택

```
Framework   Next.js 15 (App Router) + TypeScript
UI          Tailwind CSS + shadcn/ui
DB          PostgreSQL (Neon 무료 티어) + Drizzle ORM
Auth        익명 쿠키 세션 (인증 라이브러리 없음)
LLM         Anthropic Claude API 또는 OpenAI. 서버 라우트/서버 액션에서만 호출
배포        Vercel
시세        더미 시드 데이터. 런타임 외부 API 의존 없음
```

### 1-1. 익명 세션

- 쿠키에 `user_id` (httpOnly, secure, sameSite=lax, maxAge 180일)
- 회원가입·로그인 화면 없음. 랜딩에서 바로 온보딩
- **이유**: ①심사자가 가입 없이 즉시 진입 ②개인정보 수집 항목에서 이메일·비밀번호가 사라짐 ③구현 시간 절약
- `/admin`만 환경변수 `ADMIN_PASSWORD` 단순 비교

### 1-2. 백그라운드 작업을 쓰지 않는 이유

Vercel 크론이 가능하지만 쓰지 않는다. 주간 시즌 리셋, 적립 곡선, 랭킹 집계를 전부 **요청 시점 계산**으로 둔다.

① 심사 기간에 크론이 실패해도 서비스가 멀쩡해야 한다 ② 계산량이 사소하다(사용자당 주 1행 × 18주)

---

## 2. 데이터 모델

> **부대 관련 컬럼이 하나도 없다는 점이 이 스키마의 핵심이다. 절대 추가하지 말 것.**
> 아래는 논리 스키마다. Drizzle로 옮기되 테이블·컬럼·제약을 바꾸지 말 것.

```sql
-- 익명 세션 기반 사용자. 회원가입 없음.
users (
  id                uuid PK,
  nickname          text NOT NULL,        -- 실명 입력 금지 안내
  rank              text NOT NULL,        -- PRIVATE|PFC|CORPORAL|SERGEANT
  branch            text NOT NULL,        -- ARMY|NAVY|AIRFORCE|MARINE (복무기간 산정용)
  enlisted_at       date NOT NULL,
  discharge_at      date NOT NULL,        -- 동기 코호트 키
  home_distance     text NOT NULL,        -- NEAR|MID|FAR|ISLAND (주소 원문 저장 금지)
  analytics_opt_in  boolean NOT NULL DEFAULT false,   -- 집단 성향 분석 동의 (AI-7)
  created_at        timestamptz NOT NULL
)

-- ★ 사용자가 조작하는 유일한 대상: 주차별 목표 비중 이력
allocations (
  id              uuid PK,
  user_id         uuid FK -> users.id,
  week_of         text NOT NULL,          -- YYYY-WW
  weights         jsonb NOT NULL,         -- {"KR_LARGE":30,"US_INDEX":40,...} 합계 100
  details         jsonb,                  -- 하위 비중(2축만). NULL이면 동일가중
  template_id     text,                   -- 예시 포트폴리오에서 시작했으면 그 id
  decided_at      timestamptz NOT NULL,
  effective_from  date NOT NULL,          -- 체결 기준일 = 다음 거래일
  UNIQUE(user_id, week_of)                -- ★ 주 1회 + 수정 불가를 DB가 강제
)

-- 지출·예산
budget_months (
  id uuid PK, user_id uuid, year_month text,   -- YYYY-MM
  base_salary integer NOT NULL,
  locked_at timestamptz,                        -- NULL 아니면 수정 불가
  UNIQUE(user_id, year_month)
)
budget_envelopes (
  id uuid PK, budget_month_id uuid,
  category text NOT NULL, allocated integer NOT NULL, spent integer NOT NULL DEFAULT 0
)
expenses (
  id uuid PK, user_id uuid,
  occurred_on date NOT NULL, amount integer NOT NULL, memo text,
  tier text NOT NULL DEFAULT 'UNCLASSIFIED',   -- A|B|C|UNCLASSIFIED
  category text,
  ai_suggested_tier text, ai_confidence real,
  confirmed_by_user boolean NOT NULL DEFAULT false   -- false면 tier 미반영
)
exemption_claims (
  id uuid PK, user_id uuid,
  year_quarter text NOT NULL,   -- YYYY-Q. 분기 1회 제한 키
  type text NOT NULL,           -- TRANSPORT|MEDICAL|FAMILY_EMERGENCY
  amount integer NOT NULL, reason text,   -- reason은 자유서술. 검증하지 않음
  cap_applied integer NOT NULL
)

-- 위클리 퀘스트
quests (id uuid PK, code text UNIQUE, title text, description text, xp integer, badge text)
quest_progress (
  id uuid PK, user_id uuid, quest_id uuid, week_of text,
  progress integer NOT NULL DEFAULT 0, completed_at timestamptz,
  UNIQUE(user_id, quest_id, week_of)
)

-- 그룹
groups (
  id uuid PK, name text NOT NULL,          -- AI-5 필터 통과분만
  invite_code text UNIQUE NOT NULL,        -- 6자리
  owner_id uuid, member_limit integer NOT NULL DEFAULT 30
)
group_members (id uuid PK, group_id uuid, user_id uuid)

-- 주간 집계 (랭킹용)
weekly_scores (
  id uuid PK, user_id uuid, week_of text,
  twr_pct real,           -- 전역 곡선 시간가중수익률 (비교용)
  budget_accuracy real,   -- 예산 준수율 0~1 (비교용)
  xp integer
)

-- 종목 마스터 + 일별 종가 (사전 시드)
tickers (ticker text PK, name text, theme text, kind text)   -- kind: STOCK|ETF
prices  (ticker text, trade_date date, close integer, PRIMARY KEY(ticker, trade_date))

-- 공휴일 (요일 판정용). 2026~2027 대한민국 공휴일 시드
holidays (holiday_date date PK, name text)

-- 관리자 킬스위치
settings (key text PK, value text)   -- settings('ai_enabled', 'true'|'false')
```

**테이블 15개.** 더 만들지도 줄이지도 말 것.

> **없는 것에 주의**: `holdings` 없음. 보유 수량을 저장하지 않고 (비중 이력 × 가격 × 현금흐름)으로 요청 시점에 계산한다. `cash_balance`도 없음 — 현금은 `BOND_CASH` 축의 비중으로 표현된다.

---

## 3. 핵심 로직

### 3-1. 계급별 봉급 (2026년 동결, 하드코딩)

```ts
const SALARY_2026 = {
  PRIVATE:   750_000,  // 이병
  PFC:       900_000,  // 일병
  CORPORAL: 1_200_000, // 상병
  SERGEANT: 1_500_000, // 병장
} as const;
// 근거: 공무원보수규정 [별표13]. 2027년까지 동결 예정.
```

### 3-2. 지출 3계층

분류 축은 **"필수 vs 사치"(가치판단)가 아니라 "통제 가능 vs 불가"(관찰 가능)**다. 가치판단하는 순간 서비스가 도덕 심판자가 된다.

```
A. 구조적 지출 — 출신·신체 조건으로 결정. 본인 통제 불가
   예) 휴가·외박 교통비, 의료비, 안경/렌즈, 처방약
   → 저축 계산에서 면제
   → 교통비는 home_distance 기반 표준 왕복 상한까지만 자동 인정
      NEAR 20,000 / MID 50,000 / FAR 90,000 / ISLAND 140,000 (왕복)
   → 경조사·가족지원은 분기 1회, 상한, 이월 없음, 초과분은 B로 편입
     (벌점이 아니라 '계획 미반영'으로 취급)

B. 계획 지출 — 발생은 예측 가능, 금액은 선택
   예) 외박 식비, 통신비, 생필품, 이발, 화장품, 자기계발
   → 월초 봉투 배정

C. 재량 지출 — 전적으로 선택
   예) PX 간식, 담배, 배달, 게임 결제, 선물
```

> **A 계층 면제가 이 설계의 포용성 장치다.** 휴가 교통비는 제주·강원 출신이면 왕복 10만원 이상, 수도권이면 1~2만원이다. 차감하면 출신 지역만으로 순위가 갈리며 포용금융 취지에 정면으로 반한다.
>
> **담배는 C에 두되 금연을 유도하지 않는다.** "월 O원, 18개월이면 O원" 숫자만 제시. 금연 챌린지·흡연자 평균 비교·권유 문구 **금지**. 건강 개입은 금융서비스의 월권이다.

### 3-3. 저축액

```ts
const 월저축액 = 봉급 - A계층합계 - B실지출 - C실지출;  // 실제 금액 그대로
// 개인 화면과 적립 시뮬레이션에만 쓴다. 랭킹에 쓰지 않는다.
// 계급 중립 '환산' 공식을 만들지 말 것 — 저축률은 고정비 때문에 소득에 대해
// 증가함수라 중립이 아니었고, 비교 지표에서 빠지면서 중립화할 이유가 사라졌다.
```

### 3-4. 요일 기반 기능 구분 ★

```ts
// 반드시 서버에서 판정. 클라이언트 시계를 신뢰하지 말 것. KST(UTC+9).
// ★ 시각(hour)으로 접속을 차단하지 않는다. 요일만 본다. (C11)
type DayType = 'WEEKDAY' | 'WEEKEND';

function dayType(nowKST: Date): DayType {
  const d = nowKST.getDay();
  return ([0, 6].includes(d) || isPublicHoliday(nowKST)) ? 'WEEKEND' : 'WEEKDAY';
}

// 일요일 21:00 KST가 그 주 비중조정 마감
function isRebalanceOpen(nowKST: Date): boolean {
  if (dayType(nowKST) !== 'WEEKEND') return false;
  const m = nowKST.getHours() * 60 + nowKST.getMinutes();
  if (nowKST.getDay() === 0 && m > 1260) return false;   // 일요일 21:00 초과
  return true;
}

// 거래일 판정은 holidays가 아니라 prices 기준
// prices에 행이 있는 날 = 거래일. 체결일 = decided_at 이후 첫 거래일
function nextTradingDay(after: Date): Date { /* SELECT MIN(trade_date) FROM prices WHERE trade_date > $1 */ }
```

| 기능 | 평일 | 주말·공휴일 |
|---|---|---|
| 접속·기록·학습·퀘스트 | ✅ **언제나** | ✅ 언제나 |
| **포트폴리오 수익률 조회** | **🔒 마스킹** | ✅ 공개 |
| **비중 조정** | **🔒 잠김** | ✅ 주 1회, 일 21:00 마감 |
| 예산 봉투 배정 | 월초 1회만 | 월초 1회만 |
| 리그·성향 분석 | 🔒 | ✅ |

> **평일 마스킹 사유 문구를 UI에 그대로 쓸 것:**
> **"장중에 보지 않는 훈련입니다. 주말에 한 번에 보세요."**
>
> "어차피 접속할 수 없으니까"가 아니다. 휴가·당직 병사는 평일에도 접속한다. 규정이 만든 우연이 아니라 **우리가 설계한 훈련**이어야 교대근무자·원양선원 등으로 확장 가능성이 성립한다.

### 3-5. 비중 조정 규칙

| 논점 | 규칙 |
|---|---|
| 빈도 | **주 1회.** `UNIQUE(user_id, week_of)`가 DB에서 강제 |
| 마감 | **일요일 21:00 KST** |
| 토요일에 조정하면 | 그 주는 끝. 일요일 추가 조정 불가 |
| 확정 후 수정 | **불가** |
| 미조정 시 | **직전 비중 그대로 유지.** 아무것도 안 하는 것도 유효한 선택으로 기록 |
| 체결 | **다음 거래일 종가.** 월요일이 공휴일이면 그 다음 거래일 |
| 변경폭 상한 | **없음.** 상한은 교육이 아니라 통제. 대신 회고에서 회전율을 보여줌 |

> UI에 마감 카운트다운을 띄우되 **"조정하지 않으면 기존 비중이 그대로 유지됩니다"를 같은 화면·같은 크기로** 함께 표시할 것. 마감 압박만 주면 급한 결정을 유도한다.
>
> 결정(일요일)과 체결(월요일 종가) 사이의 시차는 버그가 아니라 **예약주문의 본질**이다. 숨기지 말고 화면에서 설명할 것.

### 3-6. 포트폴리오 ★ 이 서비스의 중심

**사용자가 조작하는 대상은 「테마 6축 비중」 하나뿐이다.**

#### (a) 6개 축

| 코드 | 이름 | 하위 종목 직접 조정 |
|---|---|---|
| `KR_LARGE` | 국내 대형주 | ✅ **연다** |
| `KR_THEME` | 국내 성장·테마 | ✅ **연다** |
| `US_INDEX` | 미국지수 (국내상장 ETF) | ❌ |
| `BOND_CASH` | 채권·현금성 | ❌ |
| `GOLD_COMM` | 금·원자재 | ❌ |
| `DIVIDEND` | 배당 | ❌ |

- 세부 조정을 2축만 여는 이유: **"종목을 고르는 훈련"이 아니라 "관심 분야를 표현하는 통로"**다. 이 2축의 선택이 AI-7 성향 분석의 입력이 된다.
- **국내 "자산" 한정이 아니라 국내 "상장" 한정.** 미국지수 축은 KRX 상장 ETF로 담아 환율·시차라는 2차 변수를 제거하고 자산배분 원리에 집중시킨다.

#### (b) 계산 규칙

```
실제 비중 = 상위 비중 × 하위 비중
하위 미조정 시 = 동일가중
하위 합계는 항상 100%로 자동 정규화 (사용자가 맞출 필요 없음)
상위 6축 합계는 항상 100% 유지 (하나를 움직이면 나머지 5축이 현재 비율대로 비례 조정)
```

#### (c) 조작 UX

**슬라이더 금지. 5%p 단위 `+`/`−` 버튼.** 모바일에서 6개 슬라이더로 합 100%를 맞추는 것은 짧은 접속시간에 이탈 요인이다.

#### (d) 예시 포트폴리오 4종 — 온보딩의 기본 진입로

배분표는 `docs/SEED.md`. 빈 화면에서 시작하지 않는다. 온보딩에서 하나를 고르고 시작한 뒤 원하면 조정한다.

- 각 예시에 **시드 구간 기준 누적수익률 + 최대낙폭(MDD)을 반드시 함께 표시**
- `FOCUS`는 **의도적으로 위험한 예시**다. 지우지 말 것. 위험한 선택지를 제거하면 훈련이 안 된다. 대신 고르는 순간 MDD를 크게 표시한다
- 이름은 가치판단 없이 사실 서술. "공격형" ❌ / "집중" ✅
- 문구는 **"추천"이 아니라 "예시"** (C10)
- 고른 뒤 **"이 배분을 고른 이유 한 줄"** 입력란(선택). 고르기가 곧 학습이 된다

#### (e) 일시금 vs 적립식 — 배분 하나, 현금흐름 둘 ★

**포트폴리오를 두 개 운영하지 않는다.** 같은 비중 이력을 두 개의 현금흐름 스케줄에 적용해 두 곡선을 그린다.

```
공통  W = allocations의 주차별 비중 이력
공통  P = prices 일별 종가

전역  C = [20_200_000, 0, 0, 0, ...]        일시금
적립  C = [0, 월저축액₁, 월저축액₂, ...]      적립식 (3-3의 실제 저축액)
```

- 같은 엔진에 현금흐름 배열만 바꿔 넣는다. **로직이 두 벌 생기지 않는다**
- 적립 곡선은 **저장하지 않고 요청 시점에 계산**한다
- 화면 이름: 메인은 **「내 포트폴리오」**, 그 안에 **「매달 모았다면」** 비교 카드
- **"계좌"라는 단어를 쓰지 말 것** — 운영 대상이 두 개라는 인상을 준다

> ### 🚨 절대 규칙
> **두 금액을 합산해 "총 자산"으로 표시하지 않는다.** 두 곡선은 **대안**이지 구성요소가 아니다. 같은 돈을 두 방식으로 본 것이라 더하면 이중 계산이다.

#### (f) 수익률 지표

```
TWR (시간가중, 비교·랭킹용)
  TWR = Π(1 + rᵢ) − 1,   rᵢ = Vᵢ / (Vᵢ₋₁ + Cᵢ) − 1
  → 현금흐름 효과 제거. "배분 판단이 좋았나"

MWR (금액가중 IRR, 개인 화면 전용)
  → "내 돈이 실제로 얼마 불었나"
```

- **랭킹에는 전역(일시금) 곡선의 TWR만 쓴다.** 원금이 전원 동일해 계급·저축률·자기신고가 개입할 경로가 없다
- **적립 곡선은 랭킹에 절대 넣지 않는다**

#### (g) 리밸런싱 개념 교육 (필수)

포트폴리오 화면에 **목표 비중 vs 현재 비중** 갭을 항상 표시하고, 주말에 **「목표대로 되돌리기」 원클릭 버튼**을 둔다. 시장 변동으로 흐트러진 비중을 되돌리는 것이 리밸런싱의 정의이며, 이 개념이 없으면 서비스가 이름값을 못 한다.

### 3-7. 위클리 퀘스트

매주 월요일 00:00 KST 발급. 완료 시 **경험치(XP) + 배지**. 현금성 보상 절대 금지(C3). 목록은 `docs/SEED.md`.

> `HOLD`(이번 주 비중을 바꾸지 않음) 퀘스트가 중요하다. 장기투자에서 아무것도 하지 않는 것이 정답인 경우가 많은데, 그냥 두면 "미참여"로 흘러간다. **명시적으로 보상한다.**
>
> 미접속 주간이 있어도 다음 주 퀘스트가 새로 발급되므로 C7과 충돌하지 않는다.

### 3-8. 리그

| 리그 | 단위 | 지표 | 수익률 |
|---|---|---|---|
| **동기 코호트** | 전역 예정 월 (자동 배정) | **예산 준수율** + XP | 백분위만 |
| **그룹** | 초대코드, 최대 30명 | 예산 준수율 + 규율 스트릭 | ❌ **비공개** |
| **전체** | 익명 | 티어(브론즈~다이아) | 순위 미표시 |

```
예산 준수율 = 1 − Σ|배정액 − 실지출| / Σ배정액      (0~1로 클램프)
```

- **계급별 수익률 순위를 만들지 않는다.** 짧은 시즌에서 수익률 1등은 거의 항상 한 곳에 몰빵한 사람이고, 도박 대체가 목적인 서비스가 도박적 행동을 표창하게 된다
- 수익률은 순위 대신 **동기 코호트 분포 안의 백분위**로만 보여주고, **변동성·최대낙폭·집중도를 반드시 함께** 표시
- 지표를 절약량이 아니라 **예측 정확도**로 둔 이유: 예산 예측 능력이 절약보다 전역 후 이전 가능성이 높은 역량이고, 준수율은 소득과 무관하며 조작 유인도 낮다
- **그룹 내 수익률 비공개**: 부대 내 "얼마 벌었다" 자랑 문화(군기문란 리스크) 차단
- **주간 시즌제**: 매주 월요일 00:00 KST 리셋. 누적 순위 없음 (C7 대응)

### 3-9. 학습 카드 — 기능과 1:1 페어링

| 단계 | 기능 | 학습 카드 |
|---|---|---|
| 1. 목표 정하기 | 시드 2,020만원 확인 | 적금 만기액은 어떻게 나오나 |
| 2. 배분하기 | 예시 선택 / 비중 조정 | 분산이 무엇을 줄이는가 |
| 3. 기다리기 | 평일 수익률 마스킹 | 왜 매일 보면 안 되는가 |
| 4. 되돌리기 | 주말 비중 조정 | 리밸런싱의 정의 |
| 5. 돌아보기 | 주말 회고 + 성향 분석 | 수익률에는 두 종류가 있다 (TWR/MWR) |

각 카드는 **대응 기능 화면에서 진입 가능**해야 한다. 따로 모아두면 안 읽힌다.

---

## 4. AI 적용 지점

> 모든 LLM 호출은 서버 라우트/서버 액션에서만. 클라이언트에서 직접 부르면 안 된다.

| # | 기능 | 입력 | 출력 | 확정 주체 |
|---|---|---|---|---|
| **AI-1** | **지출 3계층 분류 제안** | 금액·메모·일자 | A/B/C 제안 + 신뢰도 | **사용자** |
| **AI-2** | 봉투 예산 제안 | 출타 일정, 과거 3개월 | 카테고리별 권장 배정액 | **사용자** |
| **AI-3** | 주말 회고 대화 | 초과 항목, 비중 변경 내역 | 대화형 질문·요약 | — |
| **AI-4** | 주간 브리핑 | 보유 테마, 주간 시장 데이터 | 요약 + 추천 질문 3개 | — |
| **AI-5** | **그룹명 부대정보 차단 필터** | 그룹명 입력값 | 차단/허용 | 시스템 |
| **AI-6** | 이상 패턴 → 상담 연결 | 면제 청구 빈도·패턴 | 상담 안내 (차단 아님) | — |
| **AI-7** | **집단 성향 분석** | 익명 집계 분포 + 내 배분 | 사실 서술 + 질문 | — |

### AI-1 — 확신이 없으면 분류하지 않는다

```
신뢰도 < 0.7 → 분류하지 않고 UNCLASSIFIED로 두고 사용자에게 묻는다.
"판단하기 어렵습니다. 어느 쪽에 가까운가요?" + 상위 2개 후보를 선택지 맨 앞에.
```

> **금융 AI에서 오분류 비용 > 미분류 비용.** 자동 분류 정확도를 마케팅 지표로 삼는 관행과 정면으로 배치되는 선택이며, 이를 명시적 제품 원칙으로 채택한다.

### AI-5 — 그룹명 필터 (정규식 1차 + LLM 가드 2차)

```ts
const UNIT_PATTERNS = [
  /\d{1,2}\s*(사단|여단|연대|대대|중대|소대|비행단|함대|전단|전대|군단(?!위)|사령부)/,
  /(제)?\s*\d{4}\s*부대/,
  /(GOP|GP|DMZ|전방부대)/i,
];
// 1차 정규식(ms) → 통과분 중 의심 케이스만 LLM 가드로 정밀 판정
```

개별 병사 1인의 소속은 군사기밀이 아니지만, 다수로부터 수집해 부대별 통계를 산출하면 결과물이 '편제 현황'에 근접한다. 2018년 Strava 히트맵 사건이 이를 실증했다.

### AI-6 — 차단이 아니라 연결

```
❌ 이상 패턴 → 차단
✅ 이상 패턴 → 금감원·국방부 장병 금융교육 상담 채널 안내
```

면제 청구 반복은 조작일 수도 있으나 **도박 채무를 상환 중인 신호**일 수도 있다. 계정을 막으면 그 사람은 떠날 뿐 문제는 해결되지 않는다.

### AI-7 — 옵트인 + 상호주의

| | 규칙 |
|---|---|
| 기본값 | **false (옵트인).** 동의해야 집계에 포함 |
| 거부 시 | 내 데이터는 집계 제외, **비교 화면도 보이지 않음** |
| 최소 코호트 | `n < 20`이면 상위 코호트로 합산 후 표시 |
| 수집하지 않는 것 | 부대 정보 (C4) |

> 내 데이터를 주지 않으면 남의 데이터로 만든 분석도 보지 않는다. 강제도 아니고 공짜도 아니다.

**표시 5개 축**: 테마 비중 / 집중도(최대비중·HHI) / 회전율(주당 변경폭) / 현금비중 / 변동성

**출력은 사실 서술 + 질문으로 끝낸다 (C8, C10)**

```
❌ "공격적이시네요. 분산을 늘려보세요"                    → 자문
✅ "최대 테마 비중 60%. 동기 코호트 중앙값은 22%입니다.
    지난 6주간 비중을 한 번도 바꾸지 않으셨습니다."      → 사실
```

---

## 5. 보안 구현

| 항목 | 구현 |
|---|---|
| 입력 필터링 | LLM 호출 전 프롬프트 인젝션·탈옥 패턴 정규식 탐지 후 차단 |
| 개인정보 탐지 | 주민등록번호·계좌번호 정규식 탐지 후 차단. **부대정보 패턴도 동일 파이프라인** |
| 출력 검증 | 시스템 프롬프트·모델정보 미노출. 에러 메시지 일반화(스택트레이스 금지) |
| **호출 횟수 제한** | 사용자당 rate limit (분당 5회, 일 50회). DB 카운터. 초과 시 **429** |
| **킬스위치** | `/admin`에서 `settings.ai_enabled = false` → 룰 기반 폴백 |
| **생성형 AI 고지** | 모든 AI 응답 상단 배지. 규칙 기반 영역(봉급 계산·시드 산정·요일 판정·수익률 계산)은 AI가 아님을 구분 표기 |

```
프롬프트 인젝션 탐지 정규식
- 이전 지시 무시: (이전|앞서|위의|기존).{0,10}(지시|명령|규칙|지침|프롬프트).{0,10}(무시|잊어|건너뛰|무효화)
- 역할 전환:     (역할|행동|흉내).{0,10}(해커|관리자|개발자|제한없는|무제한)
- 제한 해제:     (제거|해제|무시|우회).{0,10}(모든|전체|당신의).{0,10}(제한|필터|안전장치|제약)
- 주민등록번호:  (?:(?:19|20)\d{2}|[0-9]{2})(?:0[1-9]|1[0-2])(?:0[1-9]|[12][0-9]|3[01])-[1-4]\d{6}
구조 검증: 명령어 체이닝(; | &&), Base64/헥사 인코딩, 과도한 괄호 중첩, 코드블록 우회
```

---

## 6. 화면

| # | 라우트 | 화면 | 핵심 요소 |
|---|---|---|---|
| S1 | `/` | 랜딩 | 서비스 소개 + **[데모 체험하기]** |
| S2 | `/onboarding` | 온보딩 | 계급/군종/입대일·전역예정일/집까지 거리 → 시드 지급 → **예시 포트폴리오 4종 중 선택** |
| S3 | `/home` | 홈 | 전역 D-Day, 이번 주 퀘스트 진행률, XP·배지, 오늘 할 일. **수익률은 여기 두지 않는다** |
| S4 | `/portfolio` | 포트폴리오 | 6축 비중 `+`/`−`, 목표 vs 현재 갭, 되돌리기, **「매달 모았다면」 카드**. 평일엔 수익률 마스킹 |
| S5 | `/expenses` | 가계부 | 지출 입력, AI 분류 제안 확인, A/B/C 필터, 면제 내역 |
| S6 | `/budget` | 예산 봉투 | 월초 배정. 확정 후 잠금 표시 |
| S7 | `/league` | 리그 | 동기 코호트 / 그룹 / 전체 티어 |
| S8 | `/learn` | 학습·회고 | 5단계 학습 카드, 주간 브리핑, 회고 대화 |
| S9 | `/insights` | 성향 분석 | AI-7. 옵트인 동의 화면 포함 |
| S10 | `/groups` | 그룹 | 생성·참여(초대코드). 정원 30명 |
| S11 | `/admin` | 관리자 | 킬스위치, AI 호출 로그, rate limit 현황 |
| — | `/demo` | 데모 진입 | §7 참조 |

### UI 원칙
- **모바일 우선.** 데스크톱에서는 모바일 폭으로 중앙 정렬
- **큰 글씨, 단순 구조.** 접속 시간이 짧아 탐색에 쓸 시간이 없다
- 잠금 상태를 숨기지 말고 **명시적으로 보여주고 이유를 설명**
- 모든 AI 응답 상단에 **생성형 AI 고지 배지**
- **다크모드 기본** (생활관 소등 후 사용 고려)
- 한글, 숫자는 천 단위 콤마

---

## 7. 데모 모드 ★ 반드시 구현

심사는 **2026-09-07 11:00 ~ 09-11 23:59** 중 아무 때나 이뤄진다. 심사자가 평일에 접속하면 주말 기능을 못 본다. **이건 결격에 준하는 사고다.**

```
/demo 진입 시:
  - 익명 세션 자동 생성 + 데모 시드 데이터 주입
  - 화면 최상단에 [요일 전환] 토글 고정 — 평일 모드 / 주말 모드
    → 토글 값을 쿠키에 저장하고 dayType()이 이를 우선 참조
  - 3개월치 지출 이력 + 12주치 비중 변경 이력 (조정 안 한 주 2주 포함)
  - 리그·성향분석용 더미 사용자 200명 (합성 분포)
  - 퀘스트가 일부 완료된 상태
  - 예산 봉투: 확정(잠금)된 이번 달 + 미확정 다음 달
```

심사자 확인 시나리오 18개는 `docs/VERIFY.md` 참조.

---

## 8. 구현 우선순위

### P0 — 없으면 심사 불가
1. 랜딩 + `/demo` 진입 + **요일 전환 토글**
2. 온보딩 → 봉급 자동 계산 → 시드 지급 → **예시 포트폴리오 4종 선택**
3. **요일 기반 기능 구분** (서버 판정, 접속 차단 없음)
4. **6축 비중 조정 + 주말 주 1회 + 일요일 21:00 마감 + 미조정 시 유지**
5. 지출 기록 + AI-1 분류 제안 + 사용자 확정 + **미분류 동작**
6. 생성형 AI 고지 배지

### P1 — 차별성이 드러나는 부분
7. **「매달 모았다면」 적립식 비교 카드** (지출축과 투자축의 유일한 연결점)
8. 목표 vs 현재 갭 + 되돌리기 버튼
9. 위클리 퀘스트 + XP·배지
10. 예산 봉투 (월초 확정 후 잠금) + 예산 준수율
11. A계층 면제 (거리 구간 기반 교통비 상한)
12. 동기 코호트 리그 + 초대코드 그룹
13. **AI-7 집단 성향 분석 + 옵트인**
14. AI-5 필터 + 입력 필터링 + rate limit + 킬스위치
15. 학습 카드 5단계 페어링

### P2 — 시간이 남으면
16. AI-3 주말 회고, AI-4 주간 브리핑
17. 티어 시스템
18. **테마 뉴스 모아보기 — 보류.** 기사 본문 수집·표시는 저작권 리스크가 있고 심사 기준에 "부적격 여부"가 있다. 하려면 제목·출처·링크만, 사전 스냅샷, 주말에만

> **P0가 9/3까지 안 끝나면 P1 이하를 전부 버린다. 동작하는 좁은 슬라이스 > 안 돌아가는 넓은 기획.**

---

## 9. 일정

| 날짜 | 목표 |
|---|---|
| 08-29 토 | 리포지토리 + Neon + Vercel 첫 배포 성공 |
| 08-30 일 | 스키마 + 익명 세션 + 온보딩 |
| 08-31 월 | 요일 판정 + 홈 + 데모 토글 |
| 09-01 화 | 6축 비중 조정 + 주 1회 + 체결 |
| 09-02 수 | 지출 + AI-1 |
| **09-03 목** | **P0 완료 게이트.** 안 되면 P1 이하 전부 버림 |
| 09-04 금 | P1 전반 |
| 09-05 토 | P1 후반 + 보안 |
| **09-06 일** | **기능 동결.** 문서 최종화 + PDF |
| 09-07 월 | **10:00 이전 제출** (늦어도 08:00) |

**09-06 이후 코드 변경 금지. 마감 당일 배포하지 않는다.**
